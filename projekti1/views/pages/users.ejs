<!DOCTYPE html>
<html lang="en">
<head>
<%- include('../partials/head') %>
</head>
<body class="container">
  <header>
    <%- include('../partials/header') %>
  </header>
  <main>
    <div class="jumbotron">
      <table>
        <% users.forEach(u => { %>
        <% if(u.n !== 'j'){ %>
        <tr>
          <td><%= u.n %>&nbsp;</td>
          <td><%= u.a %></td>
         </tr>
         <% } %>
         <% }) %>
      </table>
    </div>
  </main>
  <footer>
    <%- include('../partials/footer') %>
  </footer>
</body>
</html>